/**
 * Logo component
 * @param {*} props 
 * @returns A JSX element
 */
const LOGO = (props) => {
  const {} = props;
  return <div className={`top-navbar-logo`}>LOGO</div>;
};

export default LOGO;
